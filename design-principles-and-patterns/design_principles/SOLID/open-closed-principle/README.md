# Open closed principle:

This design principle is fairly simple, this just states that a class should be designed such that it should be open for extension but closed for modification. The idea is that this would prevent new bugs from being introduced to existing functionality